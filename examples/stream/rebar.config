{erl_opts, [debug_info]}.

{deps, [
    %% livery is provided via _checkouts symlink
    {livery, {git, "https://github.com/benoitc/livery.git", {branch, "main"}}}
]}.

{shell, [
    {apps, [livery, stream]}
]}.
